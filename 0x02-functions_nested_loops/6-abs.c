#include "main.h"

/**
 * _abs - prints the absolute value of an integer.
 * @i: the int to compute its  absolute value.
 * Return: Absolute value of the int.
 */
int _abs(int i)
{
	if (i >= 0)
	{
		return (i);
	}
	return (-i);
}
